<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sorteo Online 🎉 (Broma)</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
            background-color: #f5f5f5;
        }
        h1 {
            color: #ff6b6b;
        }
        .input-group {
            margin: 15px 0;
        }
        input, button {
            padding: 10px;
            width: 100%;
            margin: 5px 0;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        button {
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
            font-weight: bold;
        }
        button:hover {
            background-color: #45a049;
        }
        #participantsList, #prizesList {
            text-align: left;
            margin: 10px 0;
        }
        #liveLink {
            margin-top: 20px;
            font-weight: bold;
            color: blue;
            word-break: break-all;
        }
        .hidden {
            display: none;
        }
    </style>
</head>
<body>
    <h1>🎊 Sorteo Online (Broma) 🎊</h1>
    
    <div class="input-group">
        <input type="text" id="participantName" placeholder="Nombre del participante">
        <button onclick="addParticipant()">Agregar Participante</button>
    </div>
    
    <div id="participantsList">
        <h3>Participantes:</h3>
        <ul id="participantsList"></ul>
    </div>
    
    <div class="input-group">
        <input type="text" id="prizeName" placeholder="Nombre del premio">
        <button onclick="addPrize()">Agregar Premio</button>
    </div>
    
    <div id="prizesList">
        <h3>Premios:</h3>
        <ul id="prizesList"></ul>
    </div>
    
    <button onclick="generateLiveLink()">Generar Enlace para el Sorteo en Vivo</button>
    <div id="liveLinkContainer" class="hidden">
        <p>Comparte este enlace con los participantes:</p>
        <p id="liveLink"></p>
    </div>
    
    <button onclick="startFakeDraw()" id="drawButton" class="hidden">🎉 SORTEAR 🎉</button>
    
    <script>
        const participants = [];
        const prizes = [];
        let liveLinkGenerated = false;
        
        function addParticipant() {
            const name = document.getElementById('participantName').value.trim();
            if (name) {
                participants.push(name);
                updateList('participantsList', participants);
                document.getElementById('participantName').value = '';
            }
        }
        
        function addPrize() {
            const prize = document.getElementById('prizeName').value.trim();
            if (prize) {
                prizes.push(prize);
                updateList('prizesList', prizes);
                document.getElementById('prizeName').value = '';
            }
        }
        
        function updateList(listId, items) {
            const list = document.getElementById(listId);
            list.innerHTML = '';
            items.forEach(item => {
                const li = document.createElement('li');
                li.textContent = item;
                list.appendChild(li);
            });
        }
        
        function generateLiveLink() {
            if (participants.length === 0 || prizes.length === 0) {
                alert("¡Agrega participantes y premios primero!");
                return;
            }
            
            // Genera un enlace único (simulado)
            const randomId = Math.random().toString(36).substring(2, 8);
            const liveLink = `${window.location.href.replace('index.html', '')}live.html?id=${randomId}`;
            
            document.getElementById('liveLink').textContent = liveLink;
            document.getElementById('liveLinkContainer').classList.remove('hidden');
            document.getElementById('drawButton').classList.remove('hidden');
            liveLinkGenerated = true;
            
            // Guarda los datos en localStorage para la página "live.html"
            localStorage.setItem(`sorteo_${randomId}`, JSON.stringify({
                participants,
                prizes,
                winner: null // Aquí puedes manipular el ganador más tarde
            }));
        }
        
        // Función para manipular el resultado (elige un ganador falso)
        function startFakeDraw() {
            if (!liveLinkGenerated) {
                alert("Primero genera el enlace en vivo.");
                return;
            }
            
            // Aquí puedes manipular el resultado. Por defecto, elige un participante al azar.
            const randomWinnerIndex = Math.floor(Math.random() * participants.length);
            const winner = participants[randomWinnerIndex];
            const prize = prizes[0]; // O elige el premio que quieras
            
            // Guarda el ganador en localStorage
            const urlParams = new URLSearchParams(window.location.search);
            const sorteoId = urlParams.get('id');
            
            const sorteoData = JSON.parse(localStorage.getItem(`sorteo_${sorteoId}`));
            sorteoData.winner = winner;
            localStorage.setItem(`sorteo_${sorteoId}`, JSON.stringify(sorteoData));
            
            alert(`¡Sorteo manipulado! El ganador es ${winner} (pero puedes cambiarlo en el código).`);
        }
    </script>
</body>
</html>